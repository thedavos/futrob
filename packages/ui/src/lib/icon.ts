/**
 * Icon contract for Futrob UI.
 * Phosphor is the canonical icon set; consumers should import icons from
 * `@phosphor-icons/react` and type props with `Icon` from `@futrob/ui`.
 */
export type { Icon, IconProps, IconWeight } from "@phosphor-icons/react";
