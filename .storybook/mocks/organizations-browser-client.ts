/** Storybook stub — re-export kept so `/mocks` stays the catalog of Storybook clients. */
export {
  OrganizationsClientError,
  configureOrganizationsStory,
  organizationsBrowserClient,
  pathForPostAuthDestination,
} from "../../apps/web/src/modules/organizations/presentation/organizations-story-client.ts";
