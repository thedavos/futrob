/** Storybook stub — re-export kept so `/mocks` stays the catalog of Storybook clients. */
export {
  TeamsClientError,
  configurePlayerStory,
  teamsBrowserClient,
} from "../../apps/web/src/modules/teams/presentation/player-story-client.ts";
